export const metadata = {
  title: 'ZippyImage - Free Image Compressor',
  description: 'Compress your images quickly and efficiently for free.',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
